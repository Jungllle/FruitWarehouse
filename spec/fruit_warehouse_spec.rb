require 'spec_helper'

describe FruitWarehouse do
  it 'has a version number' do
    expect(FruitWarehouse::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
